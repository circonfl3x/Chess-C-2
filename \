#include "chess.h"
#include <math.h>
#include <stdlib.h>
Pos *coords_to_gcoords(int x, int y) {
    int retx = floor(x/CWIDTH);
    int rety = floor(y/CWIDTH);
  Pos *ret = malloc(sizeof(Pos));
  if (!ret)
    return NULL;
  ret->gx = retx;
  ret->gy = rety;
  return ret;
}
